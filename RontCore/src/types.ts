export interface Landlord {
  name: string;
  surname: string;
  birthdayDate: string;
  country: string;
  city: string;
  zipCode: string;
  phoneNumber: string;
  email: string;
}

export interface Field {
  selectValue: string;
  textValue: string;
}
